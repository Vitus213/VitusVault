## JWT







## 密码存储

### 加盐哈希

用户注册时：

- 用加密安全的伪随机数生成器生成随机盐，盐不用加密
- 将盐和密码进行拼接
- 使用慢哈希算法：Argon2/bcrypt，因为md5,sha-1计算的太快了
- 在数据库中维护一个表，为每个用户维护其加盐哈希后的密码哈希值+随机生成的salt

用户登录时

- 从数据库读取用户的盐和哈希值
- 用同样算法处理用户输入的密码
- 比对结果
- 
