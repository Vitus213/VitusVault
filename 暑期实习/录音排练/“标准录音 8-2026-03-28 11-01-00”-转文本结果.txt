“标准录音 8”-转文本结果

时间:  2026年3月28日 11:01:00
主题:  标准录音 8

00:00
接下来我们讲一讲C group的面试脚本，面试官问你在C group里面做你你做C group在dragon OS里解决的真实问题是什么。

00:14
答在容器治理和内核系统的处理进在放在容器和系统治理的场景中，C group它解决的是进程属于哪个资源组，以及这个资源组的限制，如何做到贯穿整个进程，生命周期乃至于扩充管理整个进程的CPU限额，内存限额，

00:39
io限额以及网络限额的这个问题，没有这套能力，我们的struggle OS它只就只能够把内核，把程序跑起来，很难做到把资源管起来，不仅是对于jovi, s的内核开发者，还是对于以后的这个OS的潜在用户。

01:03
我们都需要一个能够管理并且可观测的一个系统来对jovi S进行更好的调优以及debug,我做到的是把整个资源归属，把它的迁移限制以及可观察还有用户态度串在了一起，让真空S开始具备容器化所需要的控制面基础设施。

01:33
面试官问为什么这不是一个伪文件系统demo,因为它的真实文价值不在文件名，也不在于它的文件系统，而是在于内核状态与生命周期是否打通中国S的正版实现里面我设置了C group note以及C group root,它作为内核的真相源，而pcb上的task, c group以及secret.

01:57
To文件系统，它只是暴露了文件系统接口，方便V, f, s通过C group文件系统与C group, node和root进行管理，负责更好的创建视图的投影，以及调用可用函数传递可用的参数。

02:14
呃如果它只是一个可读写的文件系统，就不需要去连接pid,也不需要去连接root和node,它只需要写一个文件系统，那它就只是一个demo,但是这一把我把它都连接起来，所以它并不是一个demo.

02:33
面试官如果问你说我工程价值，工程价值体现在哪里，因为它解决的是jovi S朝向容器化以及朝向linux, abi兼容方向走的必须补的一块基础能力工程上，资源助理，

02:50
它是一个必不可少的需要在容器隔离场景里面必须要落地的一个硬前提，如果试想一下，没有C group,它这个进程只能够被创建和调度，不能够被分组管理，我们需要一个模块化的创建调度的一个东西，就是C group,

03:13
它可以通过层级的限制给用户提供一个一致性的资源视图，而且可以方便用户对资源的管理进行分配。

03:25
它的目的在于补齐这个系统最为缺少的能力，而不是展现一个数据结构设计。

03:35
如果linux已经有了成熟的C group,那你在jovi S里面再做一遍的意义是什么？

03:42
和loop一样，关键不是linux有没有，而是draco, s自己有没有linux西group和linux自己的进程模型，权限模型，控制器体系深度绑定，它不可能原封不动的搬进jacoos,

03:57
 jovi有自己的呃内核对象和生命周期，有自己的process file system,它只是进行语义上的一个翻译和name space nine, space克隆三这套语义对用户态暴露出来，换句话说，

04:14
我只是在补齐这个S,缺少了这个基础的资源治理设置。

04:21
为什么先做secret root和node而不做C group文件接口，因为文件接口它只是一个入口，而不是系统指向源不管是profile system迁移还是SQL的出书，next base试图还是fork前配合检查，

04:39
最终都要落到某个任务属于哪棵树上的这个节点的核心事实，如果先做文件再反推内核状态，就会出现多份状态以及持续漏斗，我们要保证它是一个串行化，而且尽量保证它是原子化的，

04:52
所以我就要保证让所有的接口层都围绕着同一份数而进行内核工作状态。

05:41
为什么要做process BID, c group只做C group2文件系统不够吗？不够。因为两者的服务场景不一样。SQL2文件系统更像是管理入口，适合创建迁移配置，而process PI,

05:56
 dC group它表示的是可以去解决，我从观察者从某个单一进程的这个视角上可以去看出来，目前的这个进程是属于C group的哪里的这个问题在容器和next base场景里面，很多用户态工具首先看的就是process,

06:12
如果这里只有真实路径，没有用户投影的话，netspace的隔离就是不通的。

06:49
为什么clone into C group一定要在fork前完成，而不是进程伸出来再迁移。

06:56
如果先生出来再迁移的话，它可能在旧归属里经过了一次pid的分配，可能经过了一次限额检查，也可能在短时间内的时间内暴露给了错误的process视图，对运行时来说，这窗口是非常短，但是它羽翼是不干净的，而且没有必要。

07:15
在fog前去解析呃fd检查目标的C group和验证权限配额是保证它。

07:26
是正确的。

07:41
那到了西湖2V2是有哪些不好的问题吗？

07:46
比如V1的话，它有一些代价比较高，比如在不同controller需要各管各的一棵树，会导致同一个workload的迁移权限判断，你重在理解，而在多套层级里面来回对齐。

07:59
那在容器治理里面，大家会更希望有一个稳定的管理单元来进行统一的管理资源，所以V2才会强调统一层级。

08:10
所以并不是V1特别差，而是容器更偏向于单数的模型，到了西格V2，是不是所有问题都解决了，也不是V2解决了同一层级和不少治理复杂度，但是它更擅长做的是进程数上面的层级资源治理，而不擅长去承载所有的业务级的Q, o, c,

08:27
 q, s,比如跨容器共享预算，还有抽象边界的统一调度，或者是更复杂的业务优先级策略，它不是靠机构自己就能表达的，而且生产上有些更现实的问题，就比如controller内核版本，实验细节会带来运维成本，

08:43
比如出现了memory C group以及呃kernel memory accounting这种相关问题。

08:54
会导致残留对象堆积，回收路径变重，简陋的CPU都上去。

09:17
描述带宽上限，然后描述突发额度保证方向优先级。

09:34
Linux对应模块在C group场景下有些什么不好的地方吗？呃我觉得它并不是不好，它只是一个经过长期生产验证的成熟方案。但是它是有一些代价的，比如它有历史兼容成本，唯1control了，里面有多层级，带来复杂度，

09:50
还有抽象边界，很多服业务级的qs,还有出跨租户的预算，群星调度都有问题，这本来就不是西姑婆这一层能单独解决的，还有时间代价。

10:06
会暴露额外的运维成本。

10:11
不会把它总结成历史设计差，而是把它总结成长期眼镜，你要同时承担兼容性和抽象边界以及真实负载下的实现成本。

10:30
主要差在ctrl覆盖面上没有把多个控制器都接进来。

