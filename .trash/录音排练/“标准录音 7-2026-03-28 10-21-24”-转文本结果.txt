“标准录音 7”-转文本结果

时间:  2026年3月28日 10:21:24
主题:  标准录音 7

00:01
现在我们讲一讲和fast有关的这个fast项目的oci spec参数是怎么设计的，出于什么考量。

00:10
我们在设计oci, spec的时候，我们的核心目标是为每个函数实例构建一个有最小权限原则，而且是安全，可重复运行，隔离性的一个沙箱环境，在这里面我们会涉及到这几个比较重要的点，比如第一个就是根文件系统，

00:29
我们会把根文件系统设置为read only的，这样子的话就保证它的函数实例的环境在根本上是一致的，它不会导致因为意外写入而出现环境漂移，这种情况可以防止对基础镜像进行篡改的这种攻击。

00:48
在进程里面，我们会设置no new bridge views,还有capabilities, no new previews.它可以呃保证用户进程，呃不可以通过set uid还有set gid来获得额外的权限，就防止容器内的权限提升而影响容器外的进程。

01:09
然后对于capability里面，我们呃采取了一个白名单的策略，移除了绝大部分默认的linux capabilities,我们保证了呃函数运行所需要的必必用必要的几个。比如在网络里面有专门给他开了一个权限，让他可以访问到呃TCTTCPUDP的特权接口，呃80423那些东西。

01:35
然后我们也保证了它可以在呃里面进行Q,也可以在里面进行发送信号进行signal.呃terminal就保证它的容器的init进程，可以对别的管理进程进行停机或者关闭。然后关于linux的命名空间，我们呃启用了pid, mount, ipcuts,还有network5种核心命名空间，尤其是在开启network.

02:08
Netspace的时候，我们没有让run C去创建一个新的网络，而是通过puff字段直接指定了一个我们已经预经创建好的网络运营空间来保证我们实现了一个编排期集成网络模式的关键。

02:24
然后后面关于挂载点除了workplace挂载的根目录，我们还配置了必要的运行时文件系统，然后保证process system那些根文件系统是可读只读的，而且我们用tmpfs挂载了def,就device还有run,还有就保证为用为用户环境提供一个功能完备但是数据是存在内存里面的。

02:51
一个临时存储的环境，就保证我们spec的一个设计哲学就是默认，拒绝而且按需授权，保证每一个函数实例都运行在一个刚刚好的一个最小化的权限环境中。

03:07
下面关于mask pass and readily pass里面是为了防止信息泄露和内核篡改，mask pass主要是为了去屏蔽一些比如什么device, now这种容器内完全不可完全不应该见到，

03:24
也不应该读的一种呃地呃位置。比如process KKCUREQ kernel core,就是容器宿主机的物理内存的直接映射，还有process kiss这种包含里面包括了一堆呃内核密钥环，

03:40
还有这种乱七八糟的就system firewire这个包含了底层的固件接口。

03:47
呃如果让用户允许写入的话，就会导致它容易变砖。

03:54
Ready pass.呃这是一个只读路径，允许容器看，但是不允许改。比如关于process system里面的内核参数，它绝对不允许修改，然后process system. Trigger,这是一个特别危险的文件，

04:12
呃不能让他去写。不然比如他这边写一个B的话，就会导致系统瞬间强制重启。还有我们考量了一下，就是它的R limits,呃就限制它为1024，如果不限制的话，就可以让它疯狂的去创造一个socket的文件，socket的连接，

04:30
然后往它的半连接表里面放很多很多的。

04:39
嗯。

04:43
我们设置了安全三板斧，就是no SUID, no execute no device就防止容器逃逸。

04:53
Suid,它会忽略所有可执行文件的suidsgid就防止攻击者通过某些漏洞把一个带有suid位的恶意二进制文件放到了device中，如果没有no suid的话，它会瞬间提权为root.

05:26
No device.

05:47
为什么我们要在已经有默认配置的情况下再做这个参数配置的工作？首先分析一下，因为我们呃run C默认配置几个关键特性，以及它为什么不适合为fast适合我们的fast需求，mc里面有些默认配置就是比如为交互式shell而生，

06:05
它会它的启动一个容器会专门给一个用户一个交互的shell中断，但是fast函数它应该是非交互式的，它需要执行的是镜像中预定义的，比如cnd命令，或者是什么上命令。

06:21
而不是给用户起一个交互式的哨，它的配置应该是静态的，需要我们，因为我们只需要一个唯一的可识别的主机名，然后放在一个函数实例的平台上面进行监控。

06:37
Linux resource里面，我们这个字段就是空的，它没有为容器设置任何CPU或内存限制。

06:47
那Netflix里面，它会创建一个全新的空的网络运营空间。但这个与我们想要用通过编排继续集成它是冲突的，所以我们要对它进行一些修改，给它指定网络运营空间，然后为它指定编码的主机名为它呃取消这个交互式的shell来节省空间资源，在节省内存资源。

07:17
这个C, I, conf我是怎么设计的呢？我们C. I. Conf是存在一个标准的一个叫fast. I. S的conflict这个文件中，它的核心考量就是一个简单性，零外部依赖和标准化，

07:33
我们在里面只使用到了两个标准插件组成的插件链，一个是bridge插件，一个是ipam,一个就是负责在宿主机上面创建一个名为fast rs0的这么一个网桥，它是由所有函数实例共享的一个虚拟交换机。

07:51
它上面配置了1个IP,它是作为所有容器的网关，然后ipm这个插件，它会呃它并不是1个集，它不需要1个集中的dhcp服务器或者1个dcp的集群，我们可以直接通过data direction目录来维护它的作业文件来分配IP,这样子我们的fast平台就可以部署在任何的单机环境中，降低运维的复杂度。

08:22
我们是这个项目怎么容器怎么启动的呢？我们首先会先呃准备一个镜像，我们会先确保这个函数所需的镜像是在我们的肯定电动存储当中的，如果不存在的话，我们就会对它进行拉取，然后我们会起一个container,

08:39
但是我们是只用调用container. D的这个API,它只是一个原数据操作，我们会提交一个预期网络命名空间路径的一个O, c, I, s, back,然后ctrl D把它记录下来，而不会去启动任何的进程，

08:57
就是为了保证我们之后能够顺利的回滚，然后我们再去启动一个cni网络，我们的平台应用会直接调用cni tour,然后在网在宿主机上创建网络运营空间，与spec里面指定的是一模一样的，

09:13
然后我们会通过cni插件链完成web pair这种呃。

09:21
网卡对的一种创建，然后去通过IP的分配，这个函数会直接返回我们分配到的IP地址，然后我们会再去准备镜像，通过overface overlay fs在只读镜像层上创建一个专属于此容器的可写的容器层。

09:39
然后再用new task去准备所有的外部资源。

09:49
这个编排的集成模式给了我们两个比较大的好处，我们可以直接的控制，而且可以享受事物的完整性，在容器启动前就可以明确的获得了一个IP地址，对于我们之后实现快速的服务，注册apu的API的网关路由，还有启动后健康检查都非常重要，

10:06
然后事物完整性，整个部署过程涉及多种资源，由我们应用来统一协调异常机制处理，我们使使用了rust的scope guard的库，它实现了资源获取，获取及初始化，就是那种抵费机制，我们每当需要的一个清理的资源被成功创建后，

10:24
我们会将它注册到一个guard中哎。

10:27
定义好清理逻辑，如果deploy函数在后续任何步骤中发出发生错误并且提前返回，这个god就会以严格相反的顺序被自动触发，确保所有已分配的资源被干细胞的回滚fast,在云计算软件中，它不仅仅是无服务器，而且是一种事件驱动，高度弹反，专注于业务逻辑的计算模型。

11:01
Oci标准，它是保证整个容器行业繁荣的基石，它的核心价值在于标准化的结偶OCI定义了OCI run time和image spec.

11:19
它保证了运行时规范以及镜像规范，它定义了一整套容器镜像的打包格式，包括manifest的索引层，保证了docker打包镜像可以在podman或ctrl加D任何oci引擎上被无差无间无差别的拉取以及运行，然后关于运行时规范，它定义了一个标准的file system bundle,然后以及一个核心的config.

11:43
JSON,它会保证上层的容器引擎和底层的容器运行时之间能够顺利的连接运行，oci的意义在于它通过划分划定清晰的边界，避免了容器技术碎片化和厂商锁定。

12:22
容器的文件系统是一个基于。

12:40
容器文件系统是一个基于union file system的一个分层的可写实复制的呃虚拟文件系统，它的组成是主要有两个部分，一个部分是多层的只读的镜像层，这些层它们来自容器的镜像层层堆叠。呃包含了操作系统的依赖库，以及呃标准的自带的应用代码，它们是不可变的。

13:15
然后在最顶层有个overfile overlay file system层，它是最顶层的可写容器层，在容器时实现创建时成立，用于存放容器运行时产生的所有数据变更，它的工作原理是写实复制，当容器第一次尝试修改来自只读层的文件时overlay file system,它会拦截这个操作，将文件从只读层复制到可写层，再进行修改。

13:48
介绍一下容器的网络模型。

13:53
比较经典的是网桥模型，它会它的核心隔离技术是网络命名空间，每个容器都有自己的网络命名空间，它会拥有着完全独立的网络接口，路由表以及iptables规则。

14:08
连接技术是通过web pair1对虚拟的以太网设备，一端连接在呃虚拟的网桥上，一端连接在容器的网络命名空间里面。

14:25
外部访问会通过宿主机的apple tables直接。Net,然后容器访问外部网络的时候，会通过里面的规则将容器私有源IP转化为宿主机的公网IP.

14:38
现在规范是定义了一套标准的接口，在容器运行时可以将网络的连接管理公司委托的各个可插拔的网络插件。

14:50
它不预先创建网络，而是在spec文件指定path字段创建预先创建好命名空间，网络应该交由编排器去管理，而非交给容器运行时。

15:10
删除操作，我们会先删除容器内的进程。

15:17
这是1个异步的，我们会创建1个wait future,然后它会观察进程是否stop, stopped,然后异步向它发出1个非阻塞的，就是signal, terminal这样的1个请求信号，然后我们会在呃它自动退出，以及1个5秒计时器内，看它哪个先完成。

15:38
如果超时的话，我们对它进行一个signal Q,啊对进行一个强制终止。

15:52
我们会在它的原数据存储里面，呃在删除容器原数据的时候，我们会对container, d的原数据存储提交，通过一个API提交，让他去删除跟容器id的关联的所有配置信息，包括之前提交的O, c I, s back.

16:13
然后我们会融入植物界面系统，然后去拆除容器网络，再用我们自己的cni实现去执行cni two的delete命令。

16:23
它会拆除这个web pair,然后将容器的IP地址从ipm文件中释放，然后删除创建网络命名空间。

17:03
Signal, q是一个特权信号，它不可以被捕获，也可以也不可以被忽略，收到一个进程投递的请求时，它不应该与用户层进行商量，会直接终止该进程的终止流程，直接回收内存，关闭文件描述符啊，什么叫僵尸状态呢？

17:53
嗯是先删除进程，然后再删除容器元数据。

18:17
为什么要选择overlay file system,

18:21
因为它是可插拔的BTFS它不可联合挂载，而且它有一个著名的缺点就是它强素质及依赖，而gfs也是强素质级依赖，而device mapper它是一个快级别的存储驱动。

18:43
为什么我们要选择一个overlay file system?

18:48
呃我们需要一个可移植性，它可以在我们常见的linux工作文件系统里面工作，然后它无需对这数据文件系统进行磁盘格式化，然后overly fasten也是一个文件级别的写实复制copyright的一个系统。

19:09
而且它已经并入了尼斯主线内核，它是整个人气生态包括container和goodbye S的默认和推荐标准。

19:23
Container D是我们整个fast项目以及云原生生态中处于最核心地位的组件，它是一个容器的内核或容器操作系统，它不是面向容器的用户的工具，它只是为其他系统提供一个动力的，稳定可靠的后台守护进程。

20:16
范围明确，稳定可靠。

20:21
以及未及审核生。

20:29
Continue设计非常优雅，它撑开了API层，然后还有核心的后端，然后执行层以及底层运行时。

20:41
它主要负责管理容器的生命周期从创建到删除的全过程，管理镜像的生命周期从拉取，推送到存储，删除，而且为提供提供存储和网络。

21:12
凯特地不构建镜像，它的存在的职责是提供一个管理已经存在的镜像。

21:32
从镜像仓库拉取到推送仓库，再到本地储存储，将镜像解压为文件系统快照，他不理解docker file的语法。

21:44
Docker file会负责构建，它会解析每1个docker file的每一条指令，然后给它创建1个临时的中间容器，然后再把这个变更提交为1个新的镜像层，

21:55
所以它我们会看到每次你想拉取1个dock镜像或者想build1个docker镜像的时候，它是有很多个进度条的，所以它还分层对每一层进行build,这样子就方便你在之后想要改命令，如果你想要改这一行命令，

22:10
它就会以1个比较快的速度重新给你整合出1个新的镜像。

22:22
Docker,它有一些更高层次的网络抽象，它网络驱动服务发现，基于DNS服务发现还有端口映射，但是肯德基它只提供了一个必要的命名空间，仅此而已。

22:41
Docker提供了卷管理，但是肯德基没有。

22:47
它提供的是一套行业内提供的是一套低层次的面向机器的grpc API,而dock提供的是一套高层次，面向人类的restful API.

23:04
Docker,总结来说，docker是一个面向开发者的一体化的容器平台和工具链，但ctrl D仅仅只是一个面向系统的核心的容器运行平台生命周期管理器，它只负责单个容器的执行镜像管理和文件系统快照，而docker会负责管理容器用户体验呃镜像化构建，还有网络存储，抽象多容器编排。

23:53
Pid network mount uts I. P. C.

23:58
User see group.

24:12
嗯。

24:30
什么叫serverless架构呢？Serverless并不是指没有服务器，而是指开发者不再需要去关心和管理服务器，所有的服务器运维工作，包括资源供给，补丁扩展和高可用都由云平台或者fast平台完全托管，

24:49
serverless是一种云原生应用设计和执行模型，是一种云原生应用设计和执行模型，它通过云提供商动态管理计算资源和的分配与执行，把应用分解为更小的，更独立的单元模块，这些单元是按需执行的，

25:07
而且计费模型基于实际执行消耗统计，而不是预先分配的服务器含量，它只是一个更广泛的概念，它保它需要一个无服务器的计算部分，以及一个B, a, s这么一个的后端服务部分去专门进行按用量计费。

25:30
它允许开发者以单个函数为单位来部署以及和运运行应用逻辑。

25:49
ICU请求。

26:07
Fast是整个模型的大脑，它负责监听事件，然后管理整个实例的生命周期，通过冷启动，然后以及热启动。

26:20
还有自动伸缩。

26:36
接下来我们讲讲C group,

26:39
以一种工程化的叙事。

